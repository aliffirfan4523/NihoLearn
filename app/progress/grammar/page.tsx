import { GrammarProgressView } from "@/components/progress/GrammarProgressView";

export const metadata = {
  title: "Japanese Grammar Progress | NihoLearn",
  description: "Browse Japanese sentence patterns, grammar rules, and mastery progress.",
};

export default function GrammarProgressPage() {
  return <GrammarProgressView />;
}
