import { GrammarPatternsEngine } from "@/components/practice/grammar-patterns/GrammarPatternsEngine";

export const metadata = {
  title: "Grammar Pattern Matcher | NihoLearn",
  description: "Match conversational situations and sentences to target JLPT grammar patterns with in-depth explanations.",
};

export default function GrammarPatternsPage() {
  return <GrammarPatternsEngine />;
}
