import { ShadowingEngine } from "@/components/practice/shadowing/ShadowingEngine";

export const metadata = {
  title: "Shadowing Companion | NihoLearn",
  description: "Synchronized line-by-line Japanese conversational audio shadowing with karaoke highlighting and voice recording.",
};

export default function ShadowingPracticePage() {
  return <ShadowingEngine />;
}
